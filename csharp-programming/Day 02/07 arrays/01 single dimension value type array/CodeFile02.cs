namespace csharp_programming {
    class Program {
        static void Main() {
            const int items = 3;
            int[] u = new int[items] { 1, 2, 3 };
        }
    }
}

/*
 * Initializers must be exact in number as that of number of elements in an array.
 * If initializers are more or less than the number of elements in the array then it's an error.
 */