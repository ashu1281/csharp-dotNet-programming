namespace csharp_programming {
    static class Algorithm {
        // Returns the smaller of the given values.
        // The smaller of a and b. If the values are equivalent, returns b.
        public static int Min(int a, int b) {
            return a < b ? a : b;
        }
    }
}
